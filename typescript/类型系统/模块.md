### 模块的定义
1. 包含顶级import或export的文件被视为模块，否则为普通脚本
2. 模块有单独的作用域，脚本在全局作用域中

### 导入类型
```typescript
// import type语法
import type { Cat, Dog } from "./animal.js";

// 内联方式
import { createCatName, type Cat, type Dog } from "./animal.js";
```